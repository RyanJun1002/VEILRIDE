{
  "name": "mistline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "export:models": "vite build --ssr tools/export-models.mjs --outDir .model-export-build --emptyOutDir && node .model-export-build/export-models.js"
  },
  "dependencies": {
    "peerjs": "^1.5.5",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "@types/three": "^0.179.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.1"
  }
}
